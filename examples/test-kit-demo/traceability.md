# Requirement-to-Test Matrix: Posts API (demo)

> Generated by `speckit.traceability.matrix`. Keys on the neutral `REQ-NNN` id,
> read from each framework's native marker form (Robot `[Tags]`, Playwright
> `@pytest.mark.req` + `# REQ:` comment). Framework-neutral by construction.

| Requirement | Name | Robot test | Playwright test | Status |
|-------------|------|------------|-----------------|--------|
| REQ-001 | fetch existing post | posts.robot::Fetch Existing Post Returns 200 With Fields | test_posts.py::test_fetch_existing_post_returns_200_with_fields | ✅ |
| REQ-002 | create post | posts.robot::Create Post Returns 201 With Id | test_posts.py::test_create_post_returns_201_with_id | ✅ |
| REQ-003 | missing post → 404 | posts.robot::Fetch Missing Post Returns 404 | test_posts.py::test_fetch_missing_post_returns_404 | ✅ |

## Gaps

- **Uncovered requirements:** none.
- **Orphan markers** (in tests but not in spec): none.

## Summary

- **Requirement coverage:** 3/3 (100%) — meets a 100% threshold.
- Coverage here is **requirement coverage**, not code coverage.

## Execution evidence

Both suites were run against the live API
(`https://jsonplaceholder.typicode.com`):

- **Robot Framework** (`robotframework-requests`): `3 tests, 3 passed, 0 failed`.
- **Playwright** (`pytest-playwright`, `request` context): `3 passed in 2.41s`.

Same neutral `spec.md` + `tasks.md`; the only difference between the two runs is
the `framework:` line in the plan (`plan.robot.md` vs `plan.playwright.md`).
